<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* General body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Form container */
        .form-container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            width: 400px;
            text-align: center;
        }

        /* Header styling */
        .form-container h2 {
            margin-bottom: 20px;
            color: #333;
            font-size: 1.8em;
        }

        /* Table styling */
        table {
            width: 100%;
            margin-bottom: 20px;
        }

        td {
            padding: 10px;
            text-align: left;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1em;
        }

        /* Submit button styling */
        input[type="submit"] {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            border: none;
            color: #fff;
            font-size: 1em;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }

        /* View link styling */
        .view-link {
            display: inline-block;
            margin-top: 10px;
            text-decoration: none;
            color: #007bff;
            font-size: 0.9em;
        }

        .view-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h2>Edit User</h2>
        <form action="/updateuser" method="post">
            <input type="hidden" name="editid" value="<%= single.id %>">
            <table>
                <tr>
                    <td>Name:</td>
                    <td><input type="text" name="username" value="<%= single.name %>"></td>
                </tr>
                <tr>
                    <td>Phone:</td>
                    <td><input type="text" name="userphone" value="<%= single.phone %>"></td>
                </tr>
            </table>
            <input type="submit" value="Update">
        </form>
        <a href="/" class="view-link">View Users</a>
    </div>
</body>

</html>
