<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register User</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>body {
        background-color: #ffffff; /* Light chocolate background */
        color: #343a40; /* Dark brown text */
        font-family: 'Arial', sans-serif;
        padding: 0;
        margin: 0;
    }
    
    .container {
        max-width: 400px;
        margin-top: 80px; /* Adjusted margin for better positioning */
        padding: 20px;
    }
    
    .card {
        background-color: #f9f9f9; /* Lighter beige for the card */
        color: #343a40;
        padding: 25px;
        border-radius: 12px; /* Rounded corners for the card */
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1); /* Slight shadow effect */
    }
    
    .form-label {
        color: #343a40;
        font-weight: 500;
        margin-bottom: 8px;
    }
    
    /* Form Input Fields */
    .form-control {
        background-color: #ffffff;
        border: 1px solid #BCAAA4;
        color: #343a40;
        padding: 12px 16px;
        font-size: 16px;
        border-radius: 8px; /* Rounded input fields */
        transition: border-color 0.3s, box-shadow 0.3s;
    }
    
    .form-control:focus {
        background-color: #fff;
        color: #343a40;
        box-shadow: 0 0 8px rgba(141, 110, 99, 0.3); /* Soft glow on focus */
        border-color: #8D6E63; /* Match the button's color */
    }
    
    /* Button */
    .btn {
        background-color: #343a40;
        border: none;
        color: white;
        padding: 14px 16px;
        border-radius: 8px;
        font-size: 16px;
        font-weight: bold;
        transition: background-color 0.3s, box-shadow 0.3s;
        cursor: pointer;
    }
    
    .btn:hover {
        background-color: #2c2926; /* Darker shade on hover */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Light shadow on hover */
    }
    
    /* Register Link */
    .login-link {
        display: block;
        text-align: center;
        margin-top: 20px;
        color: #343a40;
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
    }
    
    .login-link:hover {
        color: #8D6E63; /* Lighter shade on hover */
        text-decoration: underline;
    }
    
    /* Responsive Design */
    @media (max-width: 576px) {
        .container {
            max-width: 100%;
            padding: 15px;
        }
    
        .card {
            padding: 20px;
        }
    
        .btn {
            padding: 12px;
        }
    
        .form-control {
            padding: 10px;
        }
    }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="text-center mb-4">Register User</h2>

        <div class="card">
            <form action="/registeruser" method="post">
                <div class="mb-3">
                    <label class="form-label">Name:</label>
                    <input type="text" name="name" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Email:</label>
                    <input type="text" name="email" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Password:</label>
                    <input type="password" name="password" class="form-control" required>
                </div>

                <button type="submit" class="btn btn w-100">Register</button>
            </form>
        </div>

        <a href="/" class="login-link">Already have an account? Login Here</a>
    </div>

</body>
</html>
